# L02 image-generation record

Mode: built-in image generation. The lossless master is stored in `_research/masters/L02/` (gitignored);
the optimized deliverable is stored in `Lectures/assets/img/L02/`.

## L02-01 — the memory wall

Use case: `illustration-story`. Generate an editorial hand-drawn 16:9 opening hero plate for an advanced
LLM inference engineering lecture. In a whimsical GPU kitchen, Serega stands between an enormous pantry
wall representing high-capacity memory and a small blazing stove representing compute. A long stream of
ingredient cards travels from the pantry to the stove for every serving, and the stove waits while the
conveyor is packed. Communicate data movement versus arithmetic without labels.

Preserve the locked Serega appearance from `_research/mascots.py`: long black hair, course-blue tunic,
exactly one forest-green Tatar tübetey, and no chef's toque. Use thick black ink on warm off-white paper,
course-blue fills, and small warm-orange accents. Green appears only on Serega's skullcap. Avoid text,
pseudo-text, logos, watermarks, gradients, shadows, 3D, photorealism, plants, repeated Serega, and any
second hat.

## L02-02 — token-blind admission

Use case: `illustration-story`. Generate a 16:9 narrative plate in the same course style. Serega measures
a countertop filled with blue memory trays while a few extremely long order scrolls consume nearly all
space; a bare-headed queue manager admits orders by ticket count and the final trays overflow. No labels.
Preserve the locked Serega design and palette; green appears only on his single tübetey. Avoid text,
pseudo-text, extra hats, logos, gradients, photorealism, and decorative server-rack imagery.

## L02-03 — predict, measure, reconcile

Use case: `illustration-story`. Generate a 16:9 closing plate in the same course style. Serega runs a
controlled experiment: a balance compares blue memory trays with an orange compute whisk; a GPU stove
processes an orderly stream; a probe connects it to a monitor containing only abstract timeline,
roofline-elbow, and memory-bar shapes. Leave the leftmost quarter relatively open. Preserve the locked
Serega design and palette; green appears only on his single tübetey. No text, pseudo-text, logos, second
hat, gradients, photorealism, or decorative circuitry.
